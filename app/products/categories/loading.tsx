import { AppLoadingScreen } from "@/components/layout/app-loading-screen";

export default function ProductCategoriesLoading() {
  return (
    <AppLoadingScreen preset="products" title="Kategoriler yükleniyor" />
  );
}
