import { AppLoadingScreen } from "@/components/layout/app-loading-screen";

export default function ProductStockMovementLoading() {
  return (
    <AppLoadingScreen preset="products" title="Stok hareketi ekranı açılıyor" />
  );
}
